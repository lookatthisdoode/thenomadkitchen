import { MenuItemImage, MenuItemNoImage } from "@/app/lib/definitions";
import { UpdateItem } from "@/app/ui/dashboard/buttons";
import { Button } from "@/app/ui/button";

export default async function StoreInfo() {
  return <section>storeinfo</section>;
}
